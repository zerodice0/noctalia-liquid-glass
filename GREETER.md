# 로그인 화면 동기화

로그인 화면은 **greetd + Noctalia Greeter**입니다. 로그인 후의 Umbriel/Noctalia와 별도 프로세스·설정·렌더러를 사용합니다. 이 도구는 Greeter 1.3.1과 Noctalia 5.0.1을 기준으로 합니다.

## 적용

기존 Greeter가 정상 작동하고, 저장소의 Python 환경과 Noctalia 런타임이 준비된 상태에서:

```sh
./glass greeter-sync
./glass greeter-status
```

현재 Noctalia의 배경화면(모니터별 포함), 라이트/다크 팔레트, 폰트, 모서리 배율을 공식 동기화 기능으로 가져옵니다. 모니터 회전·배율도 공식 동기화를 따릅니다. 현재 프로필 사진은 AccountsService를 통해 로그인 화면에서도 읽을 수 있는 아이콘 경로로 복사합니다. 홈 디렉터리가 0700이면 `~/.face`를 직접 가리키는 것만으로는 Greeter에서 읽을 수 없습니다. 홈 접근 권한은 바꾸지 않습니다.

관리자 인증창에서 승인해야 합니다. 비밀번호를 스크립트·설정·저장소에 저장하지 않습니다. Greeter 상태 디렉터리를 먼저 백업하고, `greeter.toml`에서 동기화를 가리는 **외형 관련 고정값만** 해제해 `Synced`를 사용합니다. 로그인 인증, 기본 사용자/세션, 키보드 설정은 변경하지 않습니다. 기존 전원/세션 메뉴 동작도 유지합니다. 선언형 설정의 심볼릭 링크는 덮어쓰지 않고 중단합니다.

스크립트는 Noctalia의 권한 실행 훅을 동기화 요청 동안만 바꾼 뒤 원래 값으로 돌립니다. 자동 동기화나 무암호 관리자 권한을 활성화하지 않습니다. 배경화면·테마·프로필 사진을 바꾼 뒤 명시적으로 다시 실행하세요. GPD WIN mini에서도 그 기기의 Noctalia 세션에서 실행하므로 개인 경로나 모니터명을 복사할 필요가 없습니다.

성공은 설치된 팔레트/모드와 배경화면 파일 해시 검증 후에만 보고합니다. `greeter-status`는 마지막 설치 결과이며 현재 화면을 재검사하지는 않습니다. **greetd 재시작, 로그아웃, 인증 입력 자동화는 하지 않습니다.** 다음 정상 로그인 화면에서 외관을 확인하세요. 실 로그인 화면 확인과 설치 데이터 검증은 다릅니다.

## 백업과 복구

- Greeter 전체 상태: `/var/backups/noctalia-liquid-glass-greeter/<timestamp>/state` (관리자 전용)
- 사용자 측 결과·원래 프로필 사진: `$XDG_STATE_HOME/noctalia-liquid-glass/greeter/` (기본 `~/.local/state/...`)
- Noctalia 설정 변경 직전 백업: 기존 `noctalia-liquid-glass/backups/`

Greeter 복구는 출력된 정확한 백업 시각을 사용합니다. 전체 Greeter 상태를 해당 시점으로 돌리므로 마지막 세션 선택 등도 돌아갈 수 있습니다. 복구 직전 상태도 별도 보존합니다.

```sh
pkexec "$PWD/.venv/bin/python" "$PWD/scripts/greeter_sync.py" restore <timestamp>
```

프로필 사진의 원본은 사용자 로컬 백업에 남습니다. AccountsService로 복사된 이미지는 원본과 동일하므로 일반적으로 되돌릴 필요가 없습니다. `./glass restore`는 데스크톱 테마 복구이며, Greeter/AccountsService 복구를 자동으로 수행하지 않습니다.

배경화면·프로필 사진·계정명·모니터 구성·백업 원본은 이 공개 저장소에 넣지 않습니다. 동기화 방법과 재현 가능한 도구만 관리합니다.

## 불투명도·굴절 검토 (이번 변경에는 미구현)

검토 기준 소스: [Noctalia Greeter 1.3.1](https://github.com/noctalia-dev/noctalia-greeter/tree/6379fe287bb02b0bb538ad155fe18b1bf8615daf).

| 항목 | 현재 지원 | 추가 구현 범위 |
| --- | --- | --- |
| 배경화면·색상·프로필 사진 | 공식 동기화/AccountsService | 이 도구로 적용 |
| 로그인 카드 배경 불투명도 | 독립 설정은 없지만 팔레트 RGBA로 일부 간접 조절 가능 | 자동 동기화 후 알파 보정, 또는 부위별 알파 설정화 |
| 카드 뒤 블러 | 로그인 카드에 적용되지 않음 | 배경화면 텍스처 블러 및 카드 영역 마스킹 연결 |
| Liquid Glass 굴절 | 지원 없음 | Greeter 렌더러에 배경 샘플링·가장자리 굴절·반사광 셰이더 추가 |

`src/greeter/greeter_surface.cpp`의 로그인 패널은 `SurfaceVariant` 단색 채움이며, 일부 버튼 알파만 코드에 고정되어 있습니다. 설정 스키마 `src/greeter/greeter_config_store.h`에는 카드 불투명도/굴절 필드가 없습니다. 다만 `src/render/core/color.cpp`의 색상 파서는 `#RRGGBBAA`를 지원하므로 `surface_variant` 등에 알파를 넣으면 해당 색상을 쓰는 배경을 간접 조절할 수 있습니다. 같은 색상을 쓰는 여러 요소가 함께 바뀌고, 코드에서 알파를 덮어쓰는 요소는 따르지 않습니다. 이는 소스 수준의 검토이며 실제 로그인 화면에서의 대비 검증은 아직 하지 않았습니다.

일반 블러 프로그램은 있지만 `BlurCache`는 빈 초기화 구현이고, 로그인 카드 뒤에 연결된 광학 효과가 아닙니다. Noctalia의 동기화는 팔레트를 RGB로 내보내므로 데스크톱 카드 알파가 전달되지 않습니다. 팔레트 알파 방식도 재동기화 때 유지하려면 별도 후처리가 필요합니다.

굴절은 Greeter를 별도 패치·빌드해 구현할 수 있습니다. Umbriel 패치를 그대로 적용할 수는 없습니다. 우선 팔레트 알파 방식의 가독성을 시험하고, 부위별 제어가 필요하면 배경 알파를 설정화한 뒤 정적인 배경화면에 한정해 블러/굴절을 추가하는 순서가 적절합니다. 실제 계정 인증 로직은 건드리지 않고, 별도 테스트 세션에서 글자 대비·키보드 포커스·150% 배율·GPD 성능을 검증해야 합니다. 로그인 화면은 가독성과 장애 시 복구가 중요하므로 데스크톱의 20% 배경 알파를 검증 없이 그대로 사용하지 않는 편이 좋습니다.

#!/usr/bin/env bash
set -euo pipefail
repo=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)
if [[ ! -x $repo/.venv/bin/python ]]; then
  echo 'Run: python3 -m venv .venv && .venv/bin/pip install -r requirements.txt' >&2
  exit 1
fi
exec "$repo/.venv/bin/python" "$repo/scripts/theme.py" "$@"
